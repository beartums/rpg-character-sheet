export class CharacterClass {
	id: number,
	name: string,
	attackGroupId: number,
	savingThrowGroupId: number,
	hitDieSideCount: number,
	primeRequirementIds: number[],
	attributeMinimumsObject: any; 	// Object {attributeId: minimumValue...}
}

export class CharacterRace {
	id: number,
	name: string,
	adjective: string,
	plural: string,
	attributEffects: any,
			// object {attributeId: {modValue: number, minValue: number, maxValue: number},
			// 			 ...
			// 				}
	savingThrowModsObject: any, 	// object {stId: modValue, stdId: modValue}
}

export class SavingThrowClassLevel {	// Row in the SavingThrowsTable
	classId: number,
	level: number,
	savingThrowsObject: any, 			// object {savingThrowId: value, savingThrowId: value...}
}

export class
